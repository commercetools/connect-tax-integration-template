{
  "key": "${ctpTaxCalculatorExtensionKey}",
  "destination": {
    "type": "HTTP",
    "url": "${ctpExtensionBaseUrl}"
  },
  "triggers": [
    {
      "resourceTypeId": "cart",
      "actions": ["Update"],
      "condition": "cartState=\"Frozen\""
    }
  ],
  "timeoutInMs": 2000
}
